# JMeter Raw Results

Save exported JMeter `.jtl` files here when you run the plans.

Suggested names:

- `race-before.jtl`
- `race-after.jtl`
- `resource-low-workers.jtl`
- `resource-balanced-workers.jtl`
- `async-payment-capture.jtl`

The instructor explicitly asked for `.jmx` and screenshots, so `.jtl`
files are supporting evidence, not a replacement for screenshots.

